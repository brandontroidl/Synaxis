Synaxis DefCon
──────────────

DefCon is a system for automatically raising the network's security
level in response to attacks. When DefCon level changes, services
can automatically restrict registrations, force modes on channels,
and alert operators.

Configure DefCon levels in x3.conf under the "services" section.
